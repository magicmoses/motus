[build]
builder = "nixpacks"
buildCommand = "pip install -r apps/worker/requirements.txt"

[deploy]
startCommand = "cd apps/worker && python run_pipeline.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.deploy]
cronSchedule = "0 6 * * *"
