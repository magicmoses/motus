[build]
builder = "nixpacks"

[deploy]
startCommand = "cd apps/worker && pip install -r requirements.txt && python run_pipeline.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production.deploy]
cronSchedule = "0 6 */2 * *"
